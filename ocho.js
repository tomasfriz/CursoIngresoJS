/* ingreso de alumnos, no sabemos cuantos hay, pedimos nombres, cuantos alumnos ingrese.
*/
function Mostrar()
{
    
   /* var nombres;
    var cantidad;
    var nota = 0;
    var acumulador = 0;
    var promedio;
    var continuar = "";

    while(continuar != "no")
    {
        nombres = prompt("ingrese nombre");
        cantidad = cantidad +1;
        nota = prompt("ingrese nota");
        nota = parseInt(nota);
        while(nota<0 || nota>10)
        {
             nota = prompt("ingrese nota");
             nota = parseInt(nota);
        }
        acumulador = acumulador + nota;
        continuar = prompt("no, para continuar");
    }
    promedio = acumulador / nota;
    document.write("la cantidad de alumnos es: " +cantidad);
    document.write("el promedio de las notas es: " +promedio);
    */


    /*var nombres;
    var cantidad = 0;
    var seguir= "";

    while(seguir = "NO")
    {
        nombres = prompt("ingrese los nombres de los alumnos");
        cantidad = cantidad +1;
        seguir = prompt("NO, para continuar");
    }
    seguir = document.write("los nombres son: " +seguir);
*/
}
